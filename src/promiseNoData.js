/*
function promiseNoData(promise, data, error, h){  
    return  !promise && "Click on dish to view details"     // case "0"  
      || error && <h1>ERRORRR</h1>   // case 3 
   || !data && <img src="http://www.csc.kth.se/~cristi/loading.gif"/>     // case 1
   }
   */